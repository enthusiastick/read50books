<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Read 50 Books" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name">
        <h1><%= link_to "Read 50 Books", root_path %></h1>
      </li>
    </ul>

    <section class="top-bar-section">
      <ul class="right">
        <li><%- if user_signed_in? -%><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %><%- else -%><%= link_to 'Sign Up', new_user_registration_path %></li>
        <li class="right">
          <%= link_to 'Sign In', new_user_session_path %>
        </li><%- end -%>
      </ul>
       <ul class="left">
        <%- if user_signed_in? -%><li><%= link_to 'My List', user_path(current_user) %></li><%- end -%>
      </ul>
    </section>
  </nav>

  <body>
    <p></p>
    <div class="row">
      <div class="small-10 small-centered columns">
        <% flash.each do |name, msg| -%>
          <%= content_tag :div, msg, class: name %>
        <% end -%>

        <%= yield %>
        <%= javascript_include_tag "application" %>
      </div>
    </div>
  </body>
</html>
