<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) :

      if user_signed_in?
        if @user == nil
          current_user.username + " | Read 50 Books"
        else
          @user.username + " | Read 50 Books"
        end
      else
        "Read 50 Books"
       end -%></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to 'Read 50 Books', root_path %></h1>
        </li>
        <li class="toggle-topbar menu-icon">
          <a href=>Menu</a>
        </li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li class="divider"></li>
          <% if user_signed_in? %>
            <li>
              <%= link_to 'Account Settings', edit_user_registration_path %>
            </li>
            <li class="divider"></li>
          <% end %>
          <li>
            <% if user_signed_in? %>
              <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
            <% else %>
              <%= link_to 'Sign In', new_user_session_path %>
            <% end %>
          </li>
          <li class="divider"></li>
          <li>
            <% unless user_signed_in? %>
              <%= link_to 'Sign Up', new_user_registration_path %>
            <% end %>
          </li>
        </ul>
        <ul class="left">
          <%- if user_signed_in? -%><li><%= link_to 'My List', user_path(current_user) %></li><%- end -%>
        </ul>
      </section>
    </nav>

    <div class="row">
      <% flash.each do |name, msg| -%>
        <%= content_tag :div, msg, class: name %>
      <% end -%>
      <p></p>
      <%= yield %>
      <div class="small-12 columns">
        <p class="legalese">Read 50 Books is © 2014 Eben Lowe and is available under <%= link_to 'the MIT License', 'http://opensource.org/licenses/MIT' %>.</p>
      </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
